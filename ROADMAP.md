# AI Daily Brief MVP Roadmap

## 1. 路线图目标

本路线图用于指导 AI Daily Brief 从空目录推进到可连续运行的个人 MVP。最终目标是：每天固定时间抓取 7 个指定来源，完成去重、事件聚类、摘要和排序，并将一封可追溯的中文日报发送到指定邮箱。

## 2. 交付原则

- 先跑通端到端闭环，再增加复杂能力。
- 每个阶段都有可验证的产物和退出条件。
- 优先使用 RSS、API 和公开页面。
- 保留原始链接和抓取记录，便于检查模型输出。
- 任何单个来源失败都不能阻塞日报生成。
- MVP 期间以个人实际阅读体验作为最高优先级。

## 3. 阶段总览

| 阶段 | 名称 | 目标 | 预计结果 |
|---|---|---|---|
| 0 | 项目初始化 | 建立可运行的工程骨架和配置方式 | 本地可执行项目 |
| 1 | 来源接入 | 接入 7 个来源并保存原始条目 | 可重复抓取的数据层 |
| 2 | 清洗与去重 | 规范化内容，减少重复和脏数据 | 可信的候选情报集 |
| 3 | AI 处理 | 分类、摘要、评分、可信度标记 | 结构化情报事件 |
| 4 | 日报与邮件 | 生成 HTML/纯文本日报并发送 | 端到端闭环 |
| 5 | 定时与可靠性 | 自动运行、重试、日志和告警 | 连续运行能力 |
| 6 | 一周试运行 | 用真实反馈调优质量 | MVP v1.0 |
| 7 | 后续增强 | 根据使用价值扩展功能 | 可选迭代 |

## 4. 阶段 0：项目初始化

### 目标

建立最小 Python 项目、依赖管理、目录约定和环境变量配置。

### 工作项

- 创建 `src/`、`tests/`、`templates/`、`config/`、`data/` 目录。
- 建立 `pyproject.toml` 或等价依赖文件。
- 增加 `.env.example`，包含模型、邮箱、收件人和运行配置。
- 增加统一配置加载模块。
- 增加 CLI 入口：`fetch`、`process`、`digest`、`run`。
- 建立基础日志格式和 `run_id`。

### 退出条件

- 新环境可以按 README 完成安装。
- `run` 命令可以启动一次空流程并输出结构化日志。
- 密钥不会写入代码或提交到仓库。

## 5. 阶段 1：来源接入

### 目标

让 7 个来源都能通过统一接口返回标准化候选条目。

### 工作项

- 实现统一 `SourceAdapter` 接口。
- 接入 OpenAI Blog、Anthropic Blog、Hugging Face、LangChain、LlamaIndex。
- 接入 Reddit `r/LocalLLaMA` RSS/API。
- 接入 Hacker News API，并使用 AI 关键词进行初筛。
- 为每个来源设置超时、重试、请求间隔和 User-Agent。
- 保存标题、URL、发布时间、摘要、来源和抓取时间。
- 为每个适配器添加固定样本解析测试。

### 退出条件

- 单次运行能返回各来源条目数量和错误状态。
- 单个来源不可用时，其他来源仍能完成抓取。
- 每个条目都包含有效 URL 和来源标识。

## 6. 阶段 2：清洗与去重

### 目标

将不同来源的原始条目转换为可供模型处理的候选情报。

### 工作项

- 规范化 URL，移除追踪参数。
- 清理 HTML、导航、脚本和重复段落。
- 统一时间格式并保存原始时间。
- 增加内容哈希去重。
- 增加标题相似度去重。
- 建立初版事件聚类规则：标题、实体、关键词和时间窗口。
- 保留一个主条目及其关联来源。

### 退出条件

- 同一 URL 不会重复入库。
- 同一事件的明显重复报道能够合并。
- 清洗后的内容不会包含大段导航或页面模板文本。
- 通过固定样本验证边界情况：空正文、缺失时间、重复转载和不同语言标题。

## 7. 阶段 3：AI 处理

### 目标

将候选条目转换为结构化、可解释的情报事件。

### 工作项

- 设计并固化结构化 JSON Schema。
- 实现分类：模型/产品、开源、基础设施、研究、政策、社区。
- 生成一句话摘要。
- 生成“发生了什么”和“为什么值得关注”。
- 提取公司、模型、项目和技术实体。
- 计算重要性：高、中、低。
- 标记可信度：官方确认、多源报道、单一来源、社区讨论、待确认。
- 对 Reddit/HN 内容单独标记社区性质。
- 增加模型超时、重试、JSON 解析失败和降级处理。

### 退出条件

- 所有成功处理条目都能通过 Schema 校验。
- 模型无法确认的事实不会被表述为确定事实。
- 模型失败时原始条目仍可进入低信息降级流程。
- 人工抽检摘要，事实错误率达到 PDR 目标。

## 8. 阶段 4：日报与邮件

### 目标

完成用户可直接阅读的每日中文 AI 简报。

### 工作项

- 实现日报排序：可靠度、相关性、新颖度、影响力和社区信号。
- 限制重点消息最多 10 条。
- 生成“今日结论”“重点消息”“社区信号”“今日可忽略”。
- 使用模板生成响应式 HTML 邮件。
- 同时生成纯文本版本。
- 接入 SMTP 或 Resend。
- 邮件中保留来源、时间、可信度和原始链接。
- 增加空日报模板。

### 退出条件

- 手工运行可以成功发送测试邮件。
- 桌面和移动端邮件布局可读。
- 每条重点消息都有原始链接。
- 没有新内容时仍能发送正常的空日报。

## 9. 阶段 5：定时与可靠性

### 目标

让系统每天稳定执行，不需要手工介入。

### 工作项

- 配置 Asia/Shanghai 时区和每日 08:00 任务。
- 支持本地任务计划程序或 GitHub Actions。
- 为抓取、模型处理和邮件发送分别设置重试。
- 使用日报日期和批次键保证幂等，避免重复发送。
- 记录结构化运行日志和阶段耗时。
- 增加失败邮件或其他告警方式。
- 支持手工补发指定日期日报。

### 退出条件

- 连续 3 天按时发送成功。
- 单源失败、模型失败、邮件失败都能被定位。
- 重复执行同一批次不会重复发送同一封日报。

## 10. 阶段 6：一周试运行

### 目标

用真实使用反馈确认 MVP 是否值得长期运行。

### 每日检查

- 是否漏掉明显重要的 AI 信息。
- 是否出现重复事件。
- 摘要是否忠实于原文。
- 社区内容是否被误判为事实。
- 邮件长度和阅读时间是否合适。
- 哪些来源经常产生噪音。

### 调优内容

- 调整 AI 关键词和来源权重。
- 调整重点消息数量。
- 调整分类和重要性提示词。
- 增加或删除低质量来源字段。
- 修复时间、链接和邮件展示问题。

### MVP v1.0 退出条件

- 连续 7 天运行成功率 >= 95%。
- 日报按时送达率 >= 95%。
- 重复事件率 < 10%。
- 摘要人工抽检事实错误率 < 5%。
- 用户愿意继续每天阅读该邮件。

## 11. 阶段 7：后续增强

只有在 MVP 连续使用并确认价值后再进入此阶段。

### P1

- 邮件内“有用/无用”反馈链接。
- 历史日报归档和全文搜索。
- 按公司、模型、技术方向订阅。
- 飞书、微信或 Telegram 推送。

### P2

- 事件时间线和热度曲线。
- 多模型交叉总结与事实一致性检查。
- 个人阅读行为驱动的排序调整。
- Web 管理界面和来源健康度面板。

## 12. 推荐执行顺序

实际开发按以下顺序推进：

```text
项目骨架
  -> 先接 2 个官方来源验证接口
  -> 扩展到全部 7 个来源
  -> 清洗与去重
  -> AI 结构化处理
  -> 测试邮件
  -> 定时发送
  -> 连续 7 天试运行
```

第一个可演示节点不是“所有功能完成”，而是：输入真实来源，输出一封你愿意读完的测试日报。

## 13. 任务状态约定

- `[ ]` 未开始
- `[-]` 进行中
- `[x]` 已完成
- `[!]` 被阻塞，需要外部决定或修复

每次开发结束时更新本文件对应工作项，并记录实际发现的问题和下一步任务。

## 14. 当前进度

- `[x]` 阶段 0：项目初始化（Python 骨架、配置、日志、CLI、测试）
- `[ ]` 阶段 1：来源接入

下一步从 OpenAI Blog 和 Anthropic Blog 两个官方来源开始，先验证统一 `SourceAdapter` 接口，再扩展到其余 5 个来源。
